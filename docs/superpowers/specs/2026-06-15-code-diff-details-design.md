# RPC-PMT 代码修改位置明细设计

## 目标

新增一份独立技术文档，准确列出 2026-06-15 对三个 ROOT 宏进行的代码级修改，供代码复核、版本追踪和实验结果回溯使用。

## 结构

文档先给出文件级统计和提交总览，再按十个独立修复提交展开。每项包含 commit、涉及文件、函数/代码区域、修改前行为、修改后行为、关键代码对照、原因、影响和验证。

## 范围

只描述 `4e9d497..8d9614f` 中与三个 ROOT 宏相关的修改；设计、计划和说明文档提交不计入代码差异统计。新发现但未修改的问题单独列出。

## 输出

新增 `docs/RPC_PMT_CODE_DIFF_DETAILS_2026-06-15.md`，并在 README 文档列表中增加入口。完成后提交并推送 GitHub。
